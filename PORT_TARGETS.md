# Port targets — `multimap-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `multimap` that codex-rs uses, and therefore the minimum subset that `multimap-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-81] (rank `72`, tier `direct`, status `To Do`)

**Cargo package name**: `multimap`
**Rust module name (in `use` statements)**: `multimap`

## Summary

- **1** distinct symbols imported by codex-rs
- **5** total `use` statements
- **5** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `multimap::MultiMap`

- **5 `use` site(s)** across **3 codex member crate(s)**: `codex-config`, `codex-execpolicy`, `codex-execpolicy-legacy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/requirements_exec_policy.rs:7` (`codex-config`) — `use multimap::MultiMap`
- `execpolicy-legacy/src/policy.rs:1` (`codex-execpolicy-legacy`) — `use multimap::MultiMap`
- `execpolicy-legacy/src/policy_parser.rs:9` (`codex-execpolicy-legacy`) — `use multimap::MultiMap`
- `execpolicy/src/parser.rs:2` (`codex-execpolicy`) — `use multimap::MultiMap`
- `execpolicy/src/policy.rs:14` (`codex-execpolicy`) — `use multimap::MultiMap`
